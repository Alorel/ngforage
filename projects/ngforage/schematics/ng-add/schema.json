{
  "$schema": "http://json-schema.org/schema",
  "id": "NgforageNgAddOptions",
  "title": "NgForage ng add options",
  "type": "object",
  "description": "Adds ngforage to the project",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
